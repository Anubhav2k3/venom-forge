## Packages
framer-motion | Page transitions and UI animations
react-syntax-highlighter | Code highlighting for payloads
usehooks-ts | Local storage state management
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  mono: ["'JetBrains Mono'", "monospace"],
  sans: ["'Inter'", "sans-serif"],
}

Integration:
- Core logic is client-side
- Persistence uses LocalStorage
- "GitHub Dark" theme enforced via CSS variables
